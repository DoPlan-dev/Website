# AI Prompts: Five Commands Section
## Feature 05-Commands-Section

**Feature ID**: 05-Commands-Section  
**Phase**: 01-Foundation

---

## Implementation Prompts

### Prompt 1: Create Command Card Component

```
Create a CommandCard component (src/components/ui/CommandCard.tsx) with:

Props:
- command: string (command name, e.g., "/hey")
- description: string (command description)
- example?: string (optional example usage)
- icon?: ReactNode (optional icon)

Structure:
- Icon/visual indicator (top-left, 32x32px, Cyan)
- Command name (monospace, 20px, Cyan, Semibold)
- Description text (16px, Regular, Light color)
- Example usage (optional, 14px, Mono, Cyan, in code block)

Styling:
- Background: Void (#0A0E27)
- Border: Cyan gradient (30% opacity), 1px
- Border-radius: 12px
- Padding: 32px
- Shadow: Elevation 2
- Hover: Border glow intensifies, shadow expands, translateY(-4px)

Use Tailwind CSS with design system tokens.
```

---

### Prompt 2: Create Commands Section Component

```
Create a CommandsSection component (src/components/sections/CommandsSection.tsx) that:

Structure:
- Section container with padding (80px top/bottom)
- Grid layout: 4 columns desktop, 2 tablet, 1 mobile
- Gap: 24px between cards
- 5 CommandCard components (one per command)

Commands:
1. /hey - Welcome, tutorial, and command introductions
2. /do - Capture project idea, conduct meeting, and refine
3. /plan - Generate documents & execution plan
4. /dev - Start coding with automatic completion detection
5. /sys - System control panel

Add scroll-triggered fade-in animations with 100ms stagger between cards.

Use Tailwind CSS with responsive grid utilities.
```

---

### Prompt 3: Implement Hover Interactions

```
Add interactive hover effects to CommandCard components:

Hover Effects:
- Border color: Increase opacity (30% → 50%)
- Shadow: Expand (elevation-2 → elevation-3)
- Transform: translateY(-4px)
- Cursor: pointer
- Transition: 200ms cubic-bezier(0.25, 0.46, 0.45, 0.94)

Optional Click Effects:
- Add active state (translateY(-2px))
- Add focus state for keyboard navigation

Ensure animations are smooth (60fps) and use CSS transforms for performance.
```

---

### Prompt 4: Add Scroll Animations

```
Implement scroll-triggered fade-in animations for command cards:

Animation:
- Start: opacity 0, translateY(40px)
- End: opacity 1, translateY(0)
- Duration: 600ms
- Easing: ease-out
- Stagger: 100ms between each card

Implementation:
- Use Intersection Observer API or Framer Motion
- Trigger when 25% of section in viewport
- Apply animation class when visible

Ensure animations don't impact performance and work on all devices.
```

---

### Prompt 5: Create Visual Hierarchy

```
Establish visual hierarchy for the 5 commands:

Primary Command (/hey):
- Slightly emphasized (optional larger card or glow)
- Position: First in grid

Secondary Commands (/do, /plan, /dev):
- Standard styling
- Position: Middle of grid

Tertiary Command (/sys):
- Standard styling
- Position: Last in grid

Use color and positioning to show command relationships. Ensure the flow makes sense (hey → do → plan → dev → sys).
```

---

*Generated by DoPlan CLI v1.3.4*  
*Sup-Agent: Engineering Lead*  
*Date: 2025-12-03*

