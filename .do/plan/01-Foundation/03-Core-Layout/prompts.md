# AI Prompts: Core Layout Components
## Feature 03-Core-Layout

**Feature ID**: 03-Core-Layout  
**Phase**: 01-Foundation

---

## Implementation Prompts

### Prompt 1: Create Header Component

```
Create a Header component (src/components/layout/Header.tsx) with:

Structure:
- Logo/branding on left (text or image)
- Navigation links in center: Home, Features, Documentation, GitHub
- CTA button on right: "Get Started" or "Install CLI"

Styling:
- Background: Void (#0A0E27)
- Border-bottom: Cyan 20% opacity
- Padding: 24px vertical, 32px horizontal
- Sticky position: top: 0, z-index: 100

Navigation Links:
- Font: 16px, Medium (500)
- Color: Mid (#B0BBCC) default, Cyan (#00D9FF) on hover
- Spacing: 32px gap between links

Use Tailwind CSS with design system tokens. Ensure accessibility (semantic HTML, ARIA labels).
```

---

### Prompt 2: Create Footer Component

```
Create a Footer component (src/components/layout/Footer.tsx) with:

Structure:
- Footer links section (grid layout):
  - Documentation
  - Features
  - GitHub
  - Legal (Privacy, Terms)
- Copyright notice: "© 2025 DoPlan. All rights reserved."

Styling:
- Background: Void (#0A0E27)
- Border-top: Cyan 20% opacity
- Padding: 64px top, 32px bottom
- Text color: Mid (#B0BBCC)
- Links: 14px, hover to Cyan

Layout:
- Desktop: Grid with auto-fit columns (min 200px)
- Mobile: Single column

Use Tailwind CSS with design system tokens.
```

---

### Prompt 3: Create Layout Wrapper Component

```
Create a Layout wrapper component (src/components/layout/Layout.tsx) that:

Structure:
- Combines Header, main content area, Footer
- Uses flexbox for vertical layout
- Main content: max-width 1440px, centered, padding 32px

Props:
- children: React.ReactNode (page content)

Features:
- Min-height: 100vh (full viewport height)
- Flex layout: Header → main (flex: 1) → Footer
- Main content wrapper with id="main-content" for skip link

Export Layout component for use in pages.
```

---

### Prompt 4: Implement Mobile Menu

```
Create a MobileMenu component (src/components/layout/MobileMenu.tsx) with:

Features:
- Hamburger button (visible on mobile only, <768px)
- Slide-in menu from right (320px width)
- Overlay background (Void with 80% opacity)
- Close button (X icon)
- Navigation links (same as desktop)
- CTA button at bottom

Animations:
- Slide-in: transform translateX(100%) → translateX(0)
- Duration: 300ms, ease-out
- Overlay fade-in: opacity 0 → 1

Interactions:
- Click hamburger: Open menu
- Click overlay: Close menu
- Click close button: Close menu
- Press Escape: Close menu
- Click outside: Close menu

State Management:
- Use React useState for open/close state
- Handle click outside with useEffect and event listeners

Ensure smooth animations and proper z-index layering.
```

---

### Prompt 5: Add Skip-to-Content Link

```
Add accessibility skip-to-content link to the Header component:

Requirements:
- Position: Absolute, top: -40px (hidden by default)
- Visible on keyboard focus: top: 0
- Style: Cyan background, Void text, padding 8px 16px
- Link target: #main-content (main element in Layout)
- Z-index: 1000 (above everything)

Implementation:
- Add skip link as first element in Header
- Add id="main-content" to main element in Layout component
- Test with keyboard navigation (Tab key)
- Test with screen reader

Ensure the skip link is the first focusable element and works correctly.
```

---

### Prompt 6: Implement Sticky Header

```
Make the Header component sticky on scroll:

Requirements:
- Position: sticky, top: 0
- Z-index: 100 (above content, below modals)
- Background: Void (solid, not transparent)
- Optional: Add subtle shadow on scroll (elevation-1)

Implementation:
- Use CSS position: sticky
- Ensure header doesn't overlap content
- Test on mobile and desktop
- Test performance (should be smooth)

Optional Enhancement:
- Add scroll detection to change header appearance
- Add subtle background blur on scroll (backdrop-filter)

Ensure sticky behavior works smoothly without performance issues.
```

---

*Generated by DoPlan CLI v1.3.4*  
*Sup-Agent: Engineering Lead*  
*Date: 2025-12-03*

