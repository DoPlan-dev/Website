// Workflow automation triggers
export const triggerAutomation = (event: string, data?: Record<string, unknown>) => {
  // Workflow automation triggers
  // Integration with automation tools
  console.log('Automation triggered:', event, data)
}

